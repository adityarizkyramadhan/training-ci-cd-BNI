upstream app {
    server blue:3000 weight=9;   # 90% traffic
    server green:3000 weight=1;  # 10% traffic (canary)
}

server {
    listen 80;
    location / {
        proxy_pass http://app;
    }
}
